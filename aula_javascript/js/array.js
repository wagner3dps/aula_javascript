

// Exemplos de Arrays (Listas)


// Exibidos no BROWSER e no CONSOLE

/*

// Imprimir / Mostrar a lista inteira (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista);
console.log(lista);

*/

/*

// Imprimir / Mostrar somente a "pêra" na posição 01 da lista (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista[1]);
console.log(lista[1]);

*/

/*

// Inclui mais 1 item a lista e Imprime / Mostra a lista inteira (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
lista.push("uva");
alert(lista);
console.log(lista);

*/

/*

// Retira o "último" item da lista e Imprime / Mostra a lista inteira (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
lista.pop();
alert(lista);
console.log(lista);

*/

/*

// Imprime / Mostra a "o número de items da lista" (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista.length);
console.log(lista.length);

*/


/*

// Imprime / Mostra a "a ordem reversa da lista" (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista.reverse());
console.log(lista.reverse());

*/


/*

// Imprime / Mostra a "o primeiro elemento da lista" (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista[0]);
console.log(lista[0]);

*/


/*

// Converte a lista inteira para uma "string" (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];
alert(lista.toString());
console.log(lista.toString());

*/


/*

// Põe um separador na lista inteira com traços entre os elementos (No Browser e no Console)

var lista = ["maçã", "pêra", "laranja"];

alert(lista.join(" - "));
alert(lista.join("     "));
alert(lista.join(" / "));
alert(lista.join(" * "));

console.log(lista.join(" - "));
console.log(lista.join("     "));
console.log(lista.join(" / "));
console.log(lista.join(" * "));

*/